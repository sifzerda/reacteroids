import { normalWeapon }
  from './normalWeapon';

  import { shotgunWeapon }
  from './shotgunWeapon';

  import { machinegunWeapon }
  from './machinegunWeapon';

export const weapons = {
  normal: normalWeapon,
  shotgun: shotgunWeapon,
  machinegun: machinegunWeapon,
};